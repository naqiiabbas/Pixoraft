export default function AdminSeoPage() {
  return (
    <div>
      <h1 className="font-display text-2xl font-semibold tracking-tight">
        Site SEO
      </h1>
      <p className="mt-2 text-muted">
        Site-wide SEO management is set up in a later step (Phase 4). This
        becomes the screen for editing meta title, description, and OpenGraph
        per page.
      </p>
    </div>
  );
}
